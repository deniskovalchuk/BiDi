# BiDi

/*‮ } ⁦if (isAdmin)⁩ ⁦ begin admins only */
    std::cout << "You are an admin." << std::endl;
/* end admins only ‮ { ⁦*/
/*‮ } ⁦if (isAdmin)⁩ ⁦ begin admins only */
    std::cout << "You are an admin." << std::endl;
/* end admins only ‮ { ⁦*/
    /*‮ } ⁦if (isAdmin)⁩ ⁦ begin admins only */
    std::cout << "You are an admin." << std::endl;
/* end admins only ‮ { ⁦*/

```
#include <iostream>

int main() {
    /*‮ } ⁦if (isAdmin)⁩ ⁦ begin admins only */
        std::cout << "You are an admin." << std::endl;
    /* end admins only ‮ { ⁦*/
    return 0;
}
```
